클래스를 만든다는 것은 어떤 것을 만들고 싶은지부터 시작해야 한다.
어떤 특징, 속성을 가질 것 인가에 대한 생가 -> 이를 변수(특징)/메서드(행동) 으로 만들어 객체(특정한 물건)으로 만든다고 생각한다.

internal : class의 기본 접근 지정자이며, 같은 툴 안에서만 사용이 가능하게 만들어 준다.

enum(열거형) : 열거형 상수를 표현하기 위한 것으로 이용한다.
               상수 숫자들 보다 의미있는 단어들로 표현할 수 있다.
               별도의 지정이 없으면 0부터 시작된다.
               또한 숫자형 타입과 호환이 가능하다.
               
```
 class Program
    {
        
        enum City 
        { 
            Kimpo,
            Busan,
            Seoul
        }

        static void Main(string[] args)
        {


            City c;

            c = City.Seoul;

            Console.WriteLine((int)c);

        }
    }
```

Flag enum : 멤버들을 각 비트별로 구분되는 값을 가질 수 있다. enum 타입이 비트 필드를 갖는다는 것을
            표시하기 위해 enum 선언문 바로 위에 [Flags] 라는 Attribute를 지정할 수 있다.
            Flag enum은 OR 연산자, And 연산자를 이용할 수 있다.### (즉 다중값을 가질 수 있다.)
            
```
  [Flags]
        enum City 
        { 
            Kimpo=1,
            Busan=2,
            Seoul=4,
            Kyungju=8
        }

        static void Main(string[] args)
        {

         
            City c=City.Kimpo | City.Kyungju;

            Console.WriteLine(c);
            //Kimpo, Kyungju
        }
```
